{
  "name": "foreseer-server",
  "version": "0.1.0",
  "private": true,
  "description": "Foreseer orchestration service: epochs, receipt store, verify API, operator keys",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --write ."
  },
  "dependencies": {
    "foreseer.ts": "file:../packages/ts"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "prettier": "^3.9.6",
    "tsx": "^4.23.12",
    "typescript": "6",
    "vitest": "^4.1.10"
  }
}
