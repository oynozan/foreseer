# Foreseer TEE extension (stub)

Go Flare Compute Extension (FCE) that generates epoch seeds, resolves bets,
signs receipts, and anchors commitments onchain. Will be started from
https://github.com/flare-foundation/fce-extension-scaffold in a later session
(Phase 3 of the [root roadmap](../TODO.md)). It must reproduce the TypeScript
reference implementation in `packages/ts` byte for byte; `spec/SPEC.md` and
`spec/vectors/` are the contract. Nothing here is implemented yet.
