# Copy to .env.local and fill in.

# Foreseer server the dashboard reads from.
NEXT_PUBLIC_FORESEER_API=http://localhost:8787

# WalletConnect project id, free from https://cloud.reown.com
# Without it the wallet chooser lists installed browser wallets only.
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
